# Secrets & local credentials
.env
.env.*
*.pem
*.key
secrets/
credentials/

# Local market data / SQLite (large; provenance metadata may be committed separately)
data/*.db
data/*.sqlite
data/*.sqlite3
data/raw/
data/cache/

# Python / node leftovers if added later
__pycache__/
*.pyc
.venv/
node_modules/
.DS_Store

# Act-loop local arm (never commit)
config/LIVE_OK
config/standing_rules.json
config/mv_session.json
logs/schwab_account_snapshot.json
logs/schwab_oauth_reauth.log
tf-actloop.patch
