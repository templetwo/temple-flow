# When Anthony gets home — unblock checklist

1. **Reconnect GitHub in Cursor** (cloud agents said “Please reconnect GitHub”).
2. Tell Funds-beast: `push it` → cloud agent opens PR into https://github.com/templetwo/temple-flow
3. Optional: fix box `gh auth` (device flow was HTTP 429 earlier) for local CLI.
4. Provide **universe** (symbols) + **goldbrick / spiral-broker data paths** (or approved substitute feeds).
5. Schwab app + localhost callback (deferred; after backfill).
